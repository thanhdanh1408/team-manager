# 2026-08-14

## Kiểm tra lỗi project team-manager
- Chạy `tsc --noEmit`: **214 lỗi TypeScript**, tập trung ở `src/app/api/**` và `src/lib/db.ts`.
- Nguyên nhân gốc: project migrate Prisma → Firestore nhưng `src/lib/db.ts` (bản mới, thin wrapper trả về DocumentSnapshot/QuerySnapshot) **không khớp** với cách các API route sử dụng. Routes kỳ vọng:
  - `getDocuments<T>(collection, whereArray)` trả về **mảng data thuần** (có `.length`, `.map`, `.filter`)
  - `getDocument` trả về data thuần (truy cập `.name`, `.email` trực tiếp)
  - `createDocument` trả về object data
  - Export `countDocuments` và `COLLECTIONS` từ `@/lib/db` (hiện `COLLECTIONS` không có trong `@/constants`)
- Lỗi lẻ khác: `isActive` thừa ở admin/members/page.tsx:195; so sánh `TaskStatus` với `"pending"` (admin/page.tsx:70, TaskSummaryPDF.tsx:135); Pagination props sai ở member/tasks/page.tsx:153; recharts Tooltip formatter type ở 2 chart components.
- ESLint: 12 errors (unused vars, `no-explicit-any` trong db.ts, `Math.random()` trong render ở TeamChatPanel.tsx:146) + 2 warnings (thẻ `<img>` trong MessengerChat).
- Jest: 1/27 test fail — `rate-limit.test.ts` case "should block after max login attempts" (attempt thứ 6 vẫn allowed=true).

## ĐÃ FIX (chiều 14/8)
- **Rewrite `src/lib/db.ts`** thành rich API khớp với routes: `getDocuments<T>(col, filters, opts)` trả mảng data kèm `id`; `getDocument<T>` trả data hoặc null; `createDocument` auto `createdAt` + trả object đã tạo; `updateDocument`/`updateDocuments(col, filters, data)` auto `updatedAt`; `countDocuments` dùng aggregation query; export `COLLECTIONS` (users, tasks, evaluations, notifications, activityLogs, conversations, messages, taskComments, taskReports); `stripUndefined` tránh Firestore throw khi ghi field undefined. Dùng `db` từ `@/lib/firebase-admin`, giữ `initializeDb` để override cho test.
- Fix call site: thêm `createdAt` vào createDocument ở conversations/route.ts + conversations/[id]/messages/route.ts.
- Fix lẻ: addUser type thêm `isActive` (useStore.ts); `"pending"` → `"rejection_pending"` (admin/page.tsx, TaskSummaryPDF.tsx); Pagination props `onChange`/`total` (member/tasks/page.tsx); recharts v3 formatter trả `[String(value), String(name)]` (2 chart components).
- ESLint: xóa unused imports/vars (6 file); TeamChatPanel skeleton dùng mảng `SKELETON_WIDTHS` cố định thay `Math.random()` trong render.
- Test rate-limit: sửa test dùng `RATE_LIMIT_LOGIN_MAX` từ constants (test cũ hardcode 5, constant thực tế = 10).
- **Kết quả: tsc 0 lỗi, eslint 0 errors (còn 2 warnings `<img>` trong MessengerChat.tsx), jest 27/27 pass.**

## Kiểm tra pre-deploy (chiều 14/8)
- Env: đầy đủ 5 key (FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY, JWT_SECRET, NODE_ENV); JWT_SECRET 49 ký tự; `.env` đã trong `.gitignore`.
- **Production build (`next build`)**: ✅ **PASS** — TypeScript 4.1s, compiled successfully, 35 routes (8 admin, 5 member, 20+ API, 2 public). Có 1 warning: middleware convention deprecated (khuyên dùng "proxy" thay "middleware"), không ảnh hưởng chức năng.
- Build output: tất cả routes đúng type (○ Static, ƒ Dynamic), không có lỗi build-time.
- **Kết luận: project sẵn sàng chạy local (`npm run dev`) và deploy.**

## Local test (chiều 14/8)
- Gặp Turbopack panic khi xử lý `globals.css` — nguyên nhân: Tailwind v4 syntax `@import "tailwindcss"` + `@theme inline` không tương thích với Turbopack/PostCSS trong Next.js 16.3.0 dev mode.
- **Fix:** đổi về Tailwind v3 syntax (`@tailwind base/components/utilities`), bỏ `@theme inline`, bỏ font variables không dùng.
- **Kết quả:** ✅ dev server chạy thành công http://localhost:3000, tất cả routes hoạt động (GET / 200, /login 200, /admin 200, API endpoints 200/401 đúng logic). Response time: 1-2s lần đầu compile, <100ms cached.
- **Sẵn sàng:** test UI đầy đủ, sau đó commit & deploy.
